server:
  port: 3355
spring:
  application:
    name: cloud-config-client
  profiles:
    active: dev
  cloud:
    config:
      name: client-8001 # 配置文件名称
      label: master # 分支名称
      profile: dev # 读取后缀名称
      uri: http://CLOUD-CONFIG-CENTER # 配置中心地址

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka